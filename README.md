# human_geography

This repository was initialized locally and prepared for version control with a basic `.gitignore`.

## Getting Started

- Initialize a clean working tree with `git status`.
- Make commits on the `main` branch.
